name: Expire Crypto Payments

on:
  schedule:
    - cron: "*/30 * * * *"

permissions: {}

jobs:
  expire:
    name: Expire stale crypto payments
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: |
          curl -f -X POST \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            https://cyberise.org/api/cron/expire-crypto-payments
