{
  "buildCommand": "pnpm --filter @workspace/db run generate && pnpm --filter @workspace/cyberise run build",
  "outputDirectory": "artifacts/cyberise/dist/public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" }
  ],
  "framework": null
}
